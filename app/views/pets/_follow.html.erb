<%= form_for(current_user.relationships.build(followed_id: @pet.id), remote: true) do |f| %>
    <div><%= f.hidden_field :followed_id %></div>
    <%= f.submit "Follow", class: "medium round primary button" %>
<% end %>
