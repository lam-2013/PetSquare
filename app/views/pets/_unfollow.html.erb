<%= form_for(current_user.relationships.find_by_followed_id(@pet),
             html: { method: :delete },
             remote: true) do |f| %>
    <%= f.submit "Unfollow", class: "medium round secondary button" %>
<% end %>