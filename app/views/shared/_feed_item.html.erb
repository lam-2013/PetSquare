<li id="<%= feed_item.id %>">
  <%= link_to gravatar_for(feed_item.user), feed_item.user %>
  <%= link_to feed_item.user.name, feed_item.user %>
  ha portato
  <span class="pet">
    <% feed_item %>
    <%= link_to feed_item.pet.namePet, pet_path(feed_item.pet_id) %>
  </span>
  al
  <span class="content"><%= link_to feed_item.place.name, place_path(feed_item.place_id) %></span>
</li>
<br/>